# 数据集划分说明（可写入报告）

课程文档中提到数据集主要分为 train 和 test，但实际只提供了 UCI 原始数据集链接，因此本文在实验中对原始时间序列数据进行按时间顺序划分。具体而言，首先将分钟级家庭用电数据按天聚合，然后将最后 365 个自然日作为测试集，之前所有日期作为训练集。该划分方式符合时间序列预测任务的基本原则：模型只能利用历史数据学习，不能在训练阶段接触未来测试区间的数据。相比随机划分，时间顺序划分能够避免未来信息泄漏，并且保证长期 365 天预测任务具有完整的 Ground Truth 曲线。训练过程中的标准化器只在训练集上拟合，再应用于验证集和测试集。
